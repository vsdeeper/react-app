import {StyleSheet} from 'react-native';

export const screenBackgroundColor = '#fafafa';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: screenBackgroundColor,
  },
});
